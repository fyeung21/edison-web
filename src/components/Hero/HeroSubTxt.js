import React from "react";
import { HeroSubCont, SubTxt } from "./heroStyles";

const HeroSubTxt = () => {
    return (
        <HeroSubCont>
            <SubTxt>I am a user experience designer with marketing, fine arts, retail, finance and coding background.
            </SubTxt>
        </HeroSubCont>
    );
}

export default HeroSubTxt;